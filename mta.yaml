_schema-version: 3.3.0
ID: partner_commission
description: A simple CAP project.
version: 1.0.0
modules:
- name: partner_commission-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: partner_commission-auth
  - name: partner_commission-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: partner_commission-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: partner_commission-db
  parameters:
    buildpack: nodejs_buildpack
- name: partner_commission-destination-content
  type: com.sap.application.content
  requires:
  - name: partner_commission-destination-service
    parameters:
      content-target: true
  - name: partner_commission_html_repo_host
    parameters:
      service-key:
        name: partner_commission_html_repo_host-key
  - name: partner_commission-auth
    parameters:
      service-key:
        name: partner_commission-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: partner_commissions_partner_commission_html_repo_host
          ServiceInstanceName: partner_commission-html5-app-host-service
          ServiceKeyName: partner_commission_html_repo_host-key
          sap.cloud.service: partner_commissions
        - Authentication: OAuth2UserTokenExchange
          Name: partner_commissions_partner_commission_auth
          ServiceInstanceName: partner_commission-auth
          ServiceKeyName: partner_commission-auth-key
          sap.cloud.service: partner_commissions
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: partner_commission-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: partner_commission_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - partnercommissions.zip
      name: partnercommissions
      target-path: resources/
- name: partnercommissions
  type: html5
  path: app/partner_commissions
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: partner_commission-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: partner_commission-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: partner_commission-db
  type: com.sap.xs.hdi-container
  parameters:
    config:
      schema: PARTNER_COMM_HDI
    service: hana
    service-plan: hdi-shared
- name: partner_commission-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: partner_commission-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: partner_commission_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: partner_commission-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
